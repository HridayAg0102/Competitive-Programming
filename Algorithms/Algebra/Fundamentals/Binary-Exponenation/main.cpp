// Binary exponentiation.
// It is a trick to calculate a^n using O(logn) multiplication {instead of O(n)}.

// It also has important applications in many things unrelated to arthimetic.
// It can be used with any operation that have associative property.

// X.(Y.Z) = (X.Y).Z {like multiplication}

//